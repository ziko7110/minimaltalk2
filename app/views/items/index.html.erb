
<%= link_to '新規投稿', '/items/new',class: "item_new" %>
<div class="title">タイトル</div>
<% @items.each do |item| %>
  <%= item.title %>
  <%= item.image %>
  <%= item.nickname %>
  <%= item.likes %>
  <%= item.bads %>
<% end %>
